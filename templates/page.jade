extends layout

block title
	title= tag + " - als zeven"

block content
	block header
		header
			h1.large= tag
	block pages
		for post in pagination.files
			h2
				a(href="/" + post.path)= post.titel
			.meta.compact
				include meta/datum
				include meta/door
				include meta/deel